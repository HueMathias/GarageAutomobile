@page
@model ConstellationGarage.Pages.Categories.DetailsModel

@{
    ViewData["Title"] = "Category details";
}

<h1>Category details</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Category.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Category.Name)
        </dd>
    </dl>
</div>
<div>
    <a href="Index" class="btn btn-secondary"><i class="bi bi-arrow-left-square"></i></a>
    <a asp-page="./Edit" asp-route-id="@Model.Category?.Code" class="btn btn-primary"><i class="bi bi-pencil-square" style="color:white"></i></a>
</div>

